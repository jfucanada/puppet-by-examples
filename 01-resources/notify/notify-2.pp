notify { 'The title acts as a default message...': }

# For some reason this doesn't work
# Shouldn't it?
# https://docs.puppetlabs.com/puppet/latest/reference/lang_conditional.html#if-statements
# notify('This is also possible!')